var MagicItems = MagicItems || {};
MagicItems.Radio = MagicItems.Radio || {};

/**
 * Draws an Artifact as a tile.
 */
(function () {
    /**
     * Broadcasts messages to objects that care.
     * @constructor
     */
    MagicItems.Radio.Broadcaster = function() {

    };
})();